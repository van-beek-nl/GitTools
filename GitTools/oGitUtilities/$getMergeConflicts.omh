If not($cinst.$$execute(con('diff --name-only --diff-filter=U',pick(isclear(pPath),con(' -- "',pPath,'"'),'')),lvOutput))
	Signal error -1,lvOutput
End If

If isclear(lvOutput)
	Quit method list()
End If

Quit method split(lvOutput,kLf,kTrue)