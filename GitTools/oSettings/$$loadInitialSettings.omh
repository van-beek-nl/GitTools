If isclear(ivSettings.logLevel)
	If isclear(ivSettings.$cols.logLevel)
		Do ivSettings.$cols.$add("logLevel",kCharacter,kSimplechar)
	End If
	Calculate ivSettings.logLevel as "info"
End If

Do $cinst.$$save()